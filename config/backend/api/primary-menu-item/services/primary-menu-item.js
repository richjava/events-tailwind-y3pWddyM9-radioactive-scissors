'use strict';

/**
 * primary-menu-item service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::primary-menu-item.primary-menu-item');
